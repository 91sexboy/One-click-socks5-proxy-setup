# Byte-code from the workflow syntax checks is never part of a release.
__pycache__/
*.pyc

# Every test scratch tree is created with `mktemp -d` (tests/lib/assert.sh,
# tests/protocol/start_engine.sh), so no other generated artefact belongs here.

# tests/unit/test_xray_public_checkout.sh verifies private documents stay excluded.
.claude/
tasks/
.scratch/

/CLAUDE.md
/CONTEXT.md
/SPEC.md
/todo.md
